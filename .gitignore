Local/
data/
build/
__pycache__/
*.pyc
.env
secrets/
.claude/settings.local.json

# Machine-specific build settings (tectonic paths) — never committed
tools/local.ps1

# Machine-local Claude Code hooks (paths outside this repo)
.claude/settings.json
